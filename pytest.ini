[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test
python_functions = test_*
addopts = --verbose
# Coverage settings - uncomment to enable coverage reporting
# addopts = --verbose --cov=database --cov=core --cov-report=term-missing
